// 自動生成ファイル — 編集しないでください。
// 生成元: src/styles/vertical.css / 生成: `npm run gen:css`（tools/gen-vertical-css.mjs）
/** 縦書き組版 CSS のテキスト。buildEmakiDocument/buildPagedDocument の `css` に渡すか、
 *  <style> へ注入して tatemd 本家と一致した縦書きを得る。 */
export const verticalCss: string = "/* ==========================================================================\n   vertical.css — 縦書き組版スタイル\n   レイアウト・UI は app.css の責務。ここでは writing-mode 等の組版のみ扱う。\n   第一対象: Chrome / Safari / Firefox。Chrome 専用プロパティは使わない。\n   ========================================================================== */\n\n/* --------------------------------------------------------------------------\n   縦書きコンテナ\n   -------------------------------------------------------------------------- */\n.tategaki {\n  /* 縦書き基本設定 */\n  writing-mode: vertical-rl;\n  text-orientation: mixed;     /* 英数字は自然な向きで整列（縦中横なし） */\n  line-break: strict;          /* 禁則処理はブラウザ委譲 */\n  text-align: justify;         /* 均等割り付け（ベストエフォート） */\n\n  /* 本文フォント（明朝系優先） */\n  font-family: \"Hiragino Mincho ProN\", \"Hiragino Mincho Pro\", \"Yu Mincho\",\n    \"YuMincho\", \"ヒラギノ明朝 ProN\", \"游明朝\", serif;\n\n  /* 本らしい初期値 */\n  font-size: 1rem;\n  line-height: 2;\n  letter-spacing: 0.05em;\n\n  /* 縦書きで行が右から左へ流れるため、横スクロールで読み進める */\n  height: 100%;\n  overflow-x: auto;\n  overflow-y: hidden;\n}\n\n/* --------------------------------------------------------------------------\n   段落\n   -------------------------------------------------------------------------- */\n.tategaki p {\n  /* 段落先頭字下げ */\n  text-indent: 1em;\n  /* 縦書きでの段落間アキ（inline 方向 = 横方向）。控えめな値で本らしさを保つ */\n  margin-inline-start: 1.5em;\n}\n\n/* 先頭段落は段落アキなし（本文開始点） */\n.tategaki p:first-child {\n  margin-inline-start: 0;\n}\n\n/* --------------------------------------------------------------------------\n   見出し（縦書きで破綻しない最小限の調整）\n   -------------------------------------------------------------------------- */\n.tategaki h1,\n.tategaki h2,\n.tategaki h3,\n.tategaki h4,\n.tategaki h5,\n.tategaki h6 {\n  font-weight: bold;\n  text-indent: 0;\n  margin-inline-start: 2em;\n  margin-inline-end: 0.5em;\n}\n\n.tategaki h1 { font-size: 1.4em; letter-spacing: 0.1em; }\n.tategaki h2 { font-size: 1.2em; letter-spacing: 0.1em; }\n.tategaki h3 { font-size: 1.1em; }\n.tategaki h4,\n.tategaki h5,\n.tategaki h6 { font-size: 1em; }\n\n/* --------------------------------------------------------------------------\n   引用（blockquote）\n   -------------------------------------------------------------------------- */\n.tategaki blockquote {\n  margin-inline: 1.5em;\n  padding-inline-start: 1em;\n  border-inline-start: 3px solid #c0c0c0;\n  color: #555;\n  /* VS Code webview は blockquote に既定で var(--vscode-textBlockQuote-background)\n     を当てる（暗テーマだと濃灰の塗り）。紙背景を明示して上書きする（Web/CLI では無害）。 */\n  background: transparent;\n  font-style: normal; /* 縦書きで italic は崩れやすいため無効化 */\n}\n\n/* --------------------------------------------------------------------------\n   リスト（ul / ol）\n   -------------------------------------------------------------------------- */\n.tategaki ul,\n.tategaki ol {\n  /* 縦書きではリストマーカーの配置が崩れやすいため最小調整 */\n  margin-inline-start: 1.5em;\n  padding-inline-start: 1em;\n  list-style-position: inside;\n}\n\n.tategaki li {\n  text-indent: 0;\n}\n\n/* --------------------------------------------------------------------------\n   区切り線（hr）\n   -------------------------------------------------------------------------- */\n.tategaki hr {\n  /* 縦書きでは hr は水平線ではなく縦線になる */\n  border: none;\n  border-inline-start: 1px solid #aaa;\n  /* 高さで線の長さを指定 */\n  block-size: 5em;\n  margin-inline: 1.5em;\n}\n\n/* --------------------------------------------------------------------------\n   横組みの島: コードブロック・インラインコード・テーブル\n   縦書きコンテキスト内でこれらだけ横組みに戻す（Tier 2/3 有効化に伴う破綻回避）\n   -------------------------------------------------------------------------- */\n.tategaki pre,\n.tategaki table {\n  writing-mode: horizontal-tb;\n  text-orientation: mixed;\n  /* 縦書きコンテナの inline 方向（横）に対して最大幅を制限し、はみ出しを防ぐ */\n  max-inline-size: 80vb;\n  overflow-x: auto;\n  /* 縦書き行の間に挿入されるためブロック方向のマージンで上下アキを確保 */\n  margin-block: 1em;\n}\n\n/* --------------------------------------------------------------------------\n   画像（埋め込み／URL とも）\n   <img> は置換要素なので writing-mode では回転せず常に正立する。\n   縦書きの行長（＝コンテナ高＝用紙高/ペイン高）と幅の両方に収め、列やページから\n   はみ出さないようにする。プレビュー・Vivliostyle印刷・絵巻すべてに効く。\n   -------------------------------------------------------------------------- */\n.tategaki img {\n  display: block;\n  max-block-size: 90%; /* 縦書きでは block=横方向 → 列を取りすぎない */\n  max-inline-size: 100%; /* 縦書きでは inline=縦方向 → 高さを行長以内に */\n  block-size: auto;\n  inline-size: auto;\n  /* 縦書き行間のアキ（上下方向）。中央寄せは行長方向で auto */\n  margin-block: 1em;\n  margin-inline: auto;\n}\n\n/* --------------------------------------------------------------------------\n   インラインコード（縦書き内で最低限読める程度に）\n   -------------------------------------------------------------------------- */\n.tategaki code {\n  font-family: \"SFMono-Regular\", \"Menlo\", \"Consolas\", monospace;\n  font-size: 0.85em;\n  background-color: #f0f0f0;\n  padding: 0.1em 0.2em;\n  border-radius: 2px;\n  /* インラインコードは縦書きの流れの中に置かれるため writing-mode は継承（変更しない） */\n}\n\n/* --------------------------------------------------------------------------\n   コードブロック（pre > code）\n   -------------------------------------------------------------------------- */\n.tategaki pre {\n  margin-inline: 1em;\n  /* pre は横組み島（上の .tategaki pre ルールで horizontal-tb を設定済み） */\n  overflow-x: auto;\n  background-color: #f5f5f5;\n  padding: 0.75em 1em;\n  border-radius: 4px;\n}\n\n.tategaki pre code {\n  background-color: transparent;\n  padding: 0;\n  font-size: 0.85em;\n  /* pre 内のコードは横組みの島を継承する */\n  writing-mode: inherit;\n}\n\n/* --------------------------------------------------------------------------\n   テーブル（横組みの島として描画）\n   -------------------------------------------------------------------------- */\n.tategaki table {\n  border-collapse: collapse;\n  font-size: 0.9em;\n}\n\n.tategaki th,\n.tategaki td {\n  border: 1px solid #ccc;\n  padding: 0.4em 0.6em;\n  text-align: left;\n}\n\n/* --------------------------------------------------------------------------\n   強調\n   -------------------------------------------------------------------------- */\n.tategaki em {\n  /* italic は縦書きで崩れることがあるため font-style は維持しつつ letter-spacing で補正 */\n  font-style: normal;\n  /* 傍点代わりにテキスト装飾（Safari は -webkit- 接頭辞が要る。対応ブラウザでのみ有効） */\n  -webkit-text-emphasis: dot;\n  text-emphasis: dot;\n  -webkit-text-emphasis-position: right;\n  text-emphasis-position: right;\n}\n\n.tategaki strong {\n  font-weight: bold;\n}\n";
